{
  "name": "@huhhu/api",
  "description": "basic webapi implementation to mock peer to peer connection for huhuu prototype",
  "scripts": {
    "start": "npx ts-node src/main/index.ts",
    "local": "cross-env debug=true npx ts-node src/main/index.ts",
    "development": "concurrently --raw \"npm run local 2>&1\" \"npx ngrok start dev-api --config=ngrok.yml 2>&1\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.1",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
